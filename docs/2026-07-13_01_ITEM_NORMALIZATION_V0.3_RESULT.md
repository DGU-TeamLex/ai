# 품목 정규화 v0.3 전체 생성 결과

작성일: 2026-07-13  
원본: `raw_stock/*.DAT` 10개 파일  
규칙 버전: `item-normalization-v0.3`

## 생성 파일

| 파일 | 내용 | 행 수 |
|---|---|---:|
| `data/processed/item_alias_candidates_v0.3.parquet` | 기관·내부코드·원본명별 전체 정규화 후보 | 409,519 |
| `data/processed/stock_with_item_normalization_v0.3.parquet` | 원본 재고 행과 정규화 후보 조인 결과 | 16,265,602 |
| `data/sample/raw_stock_item_normalization_sample_1000.csv` | 최종 별칭 기준 층화 검수 표본 | 1,000 |
| `data/processed/item_normalization_v0.3_report.json` | 생성 통계와 품질 게이트 결과 | 1 |

## 수정한 주요 오분류

- `트렌탈정`의 `렌탈` 부분문자열을 대여물품으로 인식하지 않도록 수정
- `감염-행주`의 마지막 `주`를 주사제로 인식하지 않도록 수정
- 처방 오메가3 캡슐을 영양제로 강제 분류하지 않도록 수정
- `[비급여용]` 태그 제거 후 `용]`이 남지 않도록 수정
- 대여사업의 혈당검사지·란셋·알코올솜과 실제 대여 장비를 분리
- 일반 추출성분 의약품과 한방엑스제를 코드 체계 후보로 구분
- 임산부 배지와 미생물 배양배지를 구분
- `혈당측정`, `보건행정`의 마지막 `정`을 정제로 인식하지 않도록 수정
- 헤어밴드·스트레칭밴드와 의료용 밴드를 구분
- 핸드크림·화장품과 외용 의약품을 구분

## 검증 결과

| 검증 | 결과 |
|---|---:|
| 별칭 행 수 | 409,519 |
| 별칭 중복 | 0 |
| 별칭 필수키 누락 | 0 |
| 전체 재고 행 수 | 16,265,602 |
| 별칭 조인 누락 | 0 |
| Parquet metadata null | 0 |
| 품질 게이트 오류 | 0 |
| 알려진 오분류 재발 | 0 |
| 단위 및 파이프라인 테스트 | 28개 통과 |

원본 18개 컬럼은 모두 문자열로 저장했다. 소수 수량, 빈 문자열, 음수 보정값을 변환하지 않으므로 기존 정수형 Parquet에서 발생한 정밀도 손실이 없다.

## 정규화 상태

전체 별칭 기준:

| 상태 | 행 수 | 의미 |
|---|---:|---|
| `family_candidate` | 30,171 | 표준 품목군 후보까지 생성 |
| `group_candidate` | 214,969 | DB item group 후보 생성, 품목군 미확정 |
| `unresolved` | 164,379 | 오분류 방지를 위해 미확정 유지 |

전체 재고 행 기준:

| 상태 | 행 수 |
|---|---:|
| `family_candidate` | 1,060,469 |
| `group_candidate` | 11,840,292 |
| `unresolved` | 3,364,841 |

`unresolved`도 전체 파일에 보존되지만 임의의 표준명·품목군·원자재를 부여하지 않는다. 이 행은 공식 의약품/시약 마스터와 사람 검수 전까지 예측 및 원자재 리스크 운영 대상에서 제외한다.

## 다음 단계

1. 사용량 상위 `unresolved`부터 공식 의약품 코드와 제품 마스터에 연결한다.
2. 승인 컬럼이 있는 검수 정답셋을 만들고 자동 분류 precision을 측정한다.
3. 승인된 `item_family_id`에만 원자재 기본 매핑을 생성한다.
4. 제조사·모델별 재질 차이는 `canonical_item_id` override로 관리한다.
5. DB 적재 시 `candidate`, `approved`, `unresolved` 상태를 구분한다.
